# Horizon 配置说明 —— AI+教育 / 地理教育 博主版

## 🎯 你的专属配置已就绪

所有配置文件已经按照 **AI+教育 / 地理教育博主** 的需求完成定制，包括：

### 📁 项目结构

```
Horizon/
├── .env                        ← 【需要你填写 API 密钥】
├── .env.example                ← 参考模板
├── data/
│   ├── config.json             ← 【已定制】主编排配置
│   ├── config.example.json     ← 官方示例
│   ├── config.github.json      ← GitHub Actions 专用配置
│   └── presets.json            ← 信息源预设库
├── src/                        ← 源码（已下载）
├── .github/workflows/
│   └── daily-summary.yml       ← 每日自动运行工作流
├── docker-compose.yml
├── Dockerfile
└── pyproject.toml
```

### 📡 已配置的信息源总览

| 类别 | 信息源 | 数量 |
|------|--------|------|
| **AI/科技** | 量子位、新智元、机器之心、OpenAI/Anthropic/Google AI Blog、HN、r/MachineLearning 等 | 10+ |
| **教育科技** | 教育部、芥末堆、EdSurge、Chronicle、EdTech Magazine、r/edtech、r/education | 6+ |
| **地理/GIS** | GIS Lounge、Google Earth Blog、Esri Blog、r/geography、r/gis | 5 |
| **GitHub** | karpathy、HuggingFace、LangChain、Microsoft AI For Beginners 等 | 6 |
| **综合** | MIT Tech Review、Wired、Nature、OSS Insight（GitHub 趋势） | 4+ |

### 🗂️ 分类过滤（每日简报 25 条上限）

- **AI / 人工智能**：最多 6 条
- **教育 / EdTech**：最多 6 条
- **地理 / GIS**：最多 5 条
- **科技 / 开源**：最多 4 条
- **其他**：最多 3 条

---

## 🔑 现在需要你做的事情

### 第 1 步：安装 Python 和 uv

你需要 **Python 3.11+** 和 **uv** 包管理器：

```powershell
# 方式 A：安装 Python 后，用 pip 安装 uv
pip install uv

# 方式 B：用 PowerShell 一键安装 uv
powershell -c "irm https://astral.sh/uv/install.ps1 | iex"
```

### 第 2 步：填写 API 密钥

打开 `Horizon\.env` 文件，**至少填写一个 AI 提供商的密钥**：

**国内用户推荐（选一个即可）：**

```ini
# 推荐方案 1：DeepSeek（便宜、中文好、容易注册）
DEEPSEEK_API_KEY=sk-你的DeepSeek密钥
# 注册地址：https://platform.deepseek.com

# 推荐方案 2：通义千问 DashScope（阿里云，国内稳定）
DASHSCOPE_API_KEY=sk-你的阿里云密钥
# 注册地址：https://dashscope.console.aliyun.com

# 推荐方案 3：Anthropic Claude（中英文最强，需代理）
ANTHROPIC_API_KEY=sk-ant-你的Anthropic密钥
```

然后在 `data/config.json` 中修改 `ai.provider` 和 `ai.model`：

| 提供商 | provider 值 | model 示例 | api_key_env |
|--------|------------|-----------|-------------|
| DeepSeek | `"deepseek"` | `"deepseek-chat"` | `DEEPSEEK_API_KEY` |
| 通义千问 | `"ali"` | `"qwen-plus"` | `DASHSCOPE_API_KEY` |
| Anthropic | `"anthropic"` | `"claude-sonnet-4.5-20250929"` | `ANTHROPIC_API_KEY` |
| OpenAI | `"openai"` | `"gpt-4"` | `OPENAI_API_KEY` |
| 豆包 | `"doubao"` | `"doubao-pro"` | `DOUBAO_API_KEY` |

**其他可选密钥：**
```ini
# GitHub Token（强烈推荐，把 API 限制从 60次/小时提升到 5000次/小时）
GITHUB_TOKEN=ghp_你的GitHub Token
# 获取地址：https://github.com/settings/tokens → Generate new token (classic)
# 不需要勾选任何权限，空权限即可
```

### 第 3 步：安装依赖并运行

```bash
cd Horizon

# 安装依赖
uv sync

# 运行（默认 24 小时窗口）
uv run horizon

# 或指定时间窗口（如 48 小时）
uv run horizon --hours 48
```

日报将生成在 `data/summaries/` 目录下。

---

## 🚀 进阶功能

### 运行交互式配置向导

```bash
uv run horizon-wizard
```

向导会根据你的兴趣（如"AI 教育"、"地理教学"、"EdTech"）自动推荐信息源。

### 用 Docker 运行

```bash
docker compose run --rm horizon
```

### 推送到飞书/钉钉/微信

在 `.env` 中设置 webhook URL，并在 `config.json` 中启用：

```json
"webhook": {
    "enabled": true,
    "url_env": "HORIZON_WEBHOOK_URL",
    "platform": "feishu",
    "layout": "collapsible"
}
```

### 邮件订阅推送

在 `.env` 中设置 `EMAIL_PASSWORD`，在 `config.json` 中启用 `email` 配置。

### GitHub Actions 每日自动运行

1. 将项目推送到 GitHub 仓库
2. 在仓库 Settings → Secrets and variables → Actions 中添加密钥
3. 在 Settings → Pages 中启用 GitHub Pages（Source: `gh-pages` 分支）

---

## ❓ 常见问题

**Q: 为什么 Python 找不到？**
A: 你可能需要在 Windows 上从 https://python.org 下载安装 Python 3.11+，安装时勾选"Add Python to PATH"。

**Q: 如何添加自定义 RSS 源？**
A: 在 `data/config.json` 的 `sources.rss` 中添加：
```json
{ "name": "你的源名称", "url": "https://xxx/feed.xml", "enabled": true, "category": "your-category" }
```

**Q: 如何只想要中文日报？**
A: 在 `config.json` 中删除 `ai_languages` 配置，默认会根据 AI 分析内容自动生成中英双语。

---

## 🔗 参考链接

- [Horizon 官方文档](https://thysrael.github.io/Horizon/)
- [配置指南](https://thysrael.github.io/Horizon/configuration)
- [在线演示](https://thysrael.github.io/Horizon/)
- [问题反馈](https://github.com/Thysrael/Horizon/issues)
